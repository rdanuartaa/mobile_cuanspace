import 'package:flutter/material.dart';

class UmkmPage extends StatelessWidget {
  const UmkmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Halaman Makanan", style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
